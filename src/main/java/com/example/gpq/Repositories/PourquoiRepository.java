package com.example.gpq.Repositories;

import com.example.gpq.Entities.Pourquoi;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PourquoiRepository extends JpaRepository<Pourquoi, Long> {
}
